# 安全说明

Meme雷达开源版是只读研究工具，不包含钱包私钥、链上交易签名、交换或下单功能。GMGN 创建 API 所需的 Ed25519 认证密钥由本机生成并保存在本项目状态目录，只用于完成官方要求的 Agent 公钥绑定步骤；连接验证和扫描请求不会调用签名交易接口。

## API Key

- 只使用 GMGN 的只读 API Key；不要为本工具创建交易权限。
- Key 仅保存在运行本工具的电脑上，不应提交到 Git、截图或问题报告。
- 发布问题报告前，请删除日志、合约备注和任何可能识别个人的信息。

## 报告漏洞

涉及密钥泄露、任意文件读取、远程请求伪造或本机权限的问题，请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口，不要先创建公开 Issue。一般缺陷可提交公开 Issue，但务必先删除 API Key、日志、合约备注和个人路径。

## 风险边界

“可看”只表示进入人工研究列表，不表示合约安全、可以买入或能够盈利。未知数据不会按安全处理，第三方数据错误或延迟仍可能造成误判。
